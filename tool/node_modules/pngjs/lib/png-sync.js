"use strict";

let parse = require("pngjs/lib/parser-sync");
let pack = require("pngjs/lib/packer-sync");

exports.read = function (buffer, options) {
  return parse(buffer, options || {});
};

exports.write = function (png, options) {
  return pack(png, options);
};
